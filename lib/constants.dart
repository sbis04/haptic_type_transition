import 'package:flutter/material.dart';

const typeDuration = Duration(milliseconds: 50); // 50 milliseconds
const unTypeDuration = Duration(milliseconds: 10); // 10 milliseconds

const firstLevelDuration = Duration(milliseconds: 600);
const secondLevelDuration = Duration(milliseconds: 1500);
const thirdLevelDuration = Duration(milliseconds: 600);

const texts = [
  "Welcome to",
  "Haptic Type",
  "Flutter",
  "Which animates",
  "Text Size",
  "Animates Opacity",
  "Transitions Color",
];

final bgColors = [
  Colors.blue.shade900,
  Colors.purple.shade900,
  Colors.orange.shade800,
  Colors.pink,
  Colors.brown.shade100,
  Colors.green.shade900,
  Colors.deepPurple.shade900,
];

final textColors = [
  Colors.white,
  Colors.greenAccent,
  Colors.blue.shade900,
  Colors.black,
  Colors.blue.shade800,
  Colors.pinkAccent.shade100,
  Colors.yellowAccent,
];

const Color grey = Color(0xFF2e2c30);
